# Copy this file to `.env` and fill in your values, then restart the server.

# --- Email (appointment notifications) ---
# Option A: Brevo (free 300 emails/day) — https://www.brevo.com
#   Create account -> SMTP & API -> SMTP -> copy the values:
# SMTP_HOST=smtp-relay.brevo.com
# SMTP_PORT=587
# SMTP_USER=your-brevo-login@example.com
# SMTP_PASS=your-brevo-smtp-key
# MAIL_FROM=MedFlow <no-reply@yourdomain.com>

# Option B: Gmail (requires an App Password: Google Account -> Security -> 2FA -> App passwords)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=youssefhamed.medflow@gmail.com
SMTP_PASS=vope nqkj zrhj foyj
MAIL_FROM=MedFlow <youssefhamed.medflow@gmail.com>

# --- AI Doctor (optional) ---
# ANTHROPIC_API_KEY=sk-ant-...

# --- Security ---
# JWT_SECRET=change-me-in-production